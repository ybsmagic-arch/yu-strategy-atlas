import { notFound } from "next/navigation";import { requireAdmin } from "@/lib/auth";import { ThoughtForm } from "@/components/thought-form";import type {Thought} from "@/types/thought";
export default async function EditThought({params,searchParams}:{params:Promise<{id:string}>;searchParams:Promise<{error?:string}>}){const {db}=await requireAdmin();const {id}=await params;const [{data:item},{data:cases}]=await Promise.all([db.from("ymos_thoughts").select("*").eq("id",id).maybeSingle(),db.from("strategy_cases").select("id,title").order("case_date",{ascending:false})]);if(!item)notFound();const {error}=await searchParams;return <div className="mx-auto max-w-4xl px-5 py-12"><p className="eyebrow">EDIT THOUGHT</p><h1 className="mt-2 text-4xl">編輯每日思想</h1><ThoughtForm item={item as Thought} cases={cases??[]} error={error}/></div>}
