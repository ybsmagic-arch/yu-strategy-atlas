import {notFound} from "next/navigation";import {NaturalListPage} from "@/components/natural-list-page";import {levelLabels,ymosLevels} from "@/types/naturalWisdom";export default async function Level({params}:{params:Promise<{level:string}>}){const {level}=await params;if(!ymosLevels.includes(level as never))notFound();const info=levelLabels[level as keyof typeof levelLabels];return <NaturalListPage title={`${info.zh} · ${info.en}`} level={level}/>}
